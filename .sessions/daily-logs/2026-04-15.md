# Daily Log — 2026-04-15

## Frontend Webapp — Brainstorm + FE-S1 Plan

---

### Session Start

- `/resume` — loaded Sprint 12 closed state (v1 backend complete, 386 unit / 29 integration / 35 E2E)
- Deferred all 4 Phase 2 backend brainstorm ideas (monitoring, performance, security, deployment) to later
- Started `/brainstorm full webapp frontend`

---

### Brainstorm Decisions (8 questions)

| Question | Choice |
|---|---|
| Framework | Next.js 14 App Router |
| Repository layout | Separate repo `terroir-ma-web` |
| Portal scope | Two apps: `apps/portal` (all 8 staff roles) + `apps/public` (consumer QR verify) |
| UI library | shadcn/ui + Tailwind CSS |
| i18n | `next-intl` — fr (default) / ar (RTL) / zgh (Tifinagh LTR) |
| API client | `openapi-fetch` (openapi-ts codegen) + TanStack Query |
| Auth | `next-auth` v5 + Keycloak provider, role guard in middleware |
| Deployment | Docker multi-stage, added to `terroir-ma/docker-compose.yml` |

---

### Design Sections Validated

1. Repository structure — pnpm workspace, `apps/portal`, `apps/public`, `packages/api-client`
2. Portal route structure — locale-prefixed route groups per role, one login, isolated UX
3. Public app + i18n RTL strategy — Server Component QR verify, `dir="rtl"` on `<html>`, Tailwind logical properties
4. API client codegen + TanStack Query patterns — `unwrap()` helper for NestJS envelope
5. Auth flow + Docker integration — `next-auth` v5 callbacks, middleware role guard, two Docker services
6. Story breakdown + YAGNI — 10 sprints, 136 SP total; start FE-S1 → FE-S2 → FE-S8 (public QR early) → FE-S4

---

### Plan Created: FE-S1 (17 tasks, 13 SP, 6 batches)

| Batch | Scope |
|---|---|
| 1 | Repo init, pnpm workspace, `@terroir/api-client`, codegen script |
| 2 | `apps/portal` Next.js scaffold + `next-intl` + root layout (RTL) |
| 3 | `apps/public` Next.js scaffold + `next-intl` + QR verify Server Component |
| 4 | shadcn/ui in both apps + Tailwind RTL logical properties |
| 5 | Dockerfiles + `terroir-portal`/`terroir-public` in `terroir-ma/docker-compose.yml` |
| 6 | GitHub Actions CI + root tsconfig + final smoke verification |

---

### Files Created This Session

- `docs/plans/2026-04-15-frontend-webapp/design.md` — full frontend design (all 6 sections)
- `docs/plans/2026-04-15-frontend-webapp/plan.md` — FE-S1 implementation plan (17 tasks)

---

### Decisions

- Phase 2 backend brainstorms deferred — will resume when backend observability/deployment becomes priority
- Frontend is a separate repo (`terroir-ma-web`) — consumes `docs/api/openapi.json` via codegen, no direct NestJS coupling
- YAGNI order confirmed: scaffold → auth → public QR verify → cooperative-admin → rest
- Amiri font already exists at `assets/fonts/Amiri-Regular.ttf` — reusable in frontend

---

### What's Next

- `/execute` to run FE-S1 (creates `terroir-ma-web` from scratch)
- After FE-S1 completes: `/plan` for FE-S2 (next-auth + Keycloak + role guard middleware + layout shells)

---

### Session End State

- Backend: v1 complete, all sprints 1–12 closed
- Frontend: brainstorm complete, FE-S1 plan ready, repo not yet created
- No blockers
