# Daily Log — 2026-04-25

## Session Context

- **Session ID:** session-2026-04-25-P2-S5-security-performance
- **Phase:** Phase 2 — Observability & Quality (COMPLETE)

## What was done

### P2-S4: Consumer Migration + Authenticated Playwright E2E (completed earlier this session)

- Migrated all 3 remaining Kafka listeners (`CooperativeListener`, `ProductListener`, `CertificationListener`) from dormant `@EventPattern` to live `KafkaConsumerService.subscribe()` pattern matching `NotificationListener`
- Critical fix: `ProductListener` now `JSON.parse`s `testValues` from Avro wire string before persisting to JSONB
- Updated unit specs for all 3 listeners: added `KafkaConsumerService` mock, `onModuleInit` subscription tests, removed stale `{} as never` second args
- Added 7 Keycloak test users to `realm-export.json` + new `terroir-portal` confidential OIDC client
- Created Playwright E2E infrastructure: `global-setup.ts` (browser-based Keycloak auth for 7 roles), 8 role-authenticated spec files, `.github/workflows/e2e.yml`
- Fixed `.env.example`: `KEYCLOAK_ISSUER` → `http://localhost:8080/realms/terroir-ma`, `KEYCLOAK_CLIENT_SECRET` → `terroir-portal-secret`

### P2-S5: OWASP ZAP Security Scanning + k6 Performance Testing (completed this session)

**Batch 1 — ZAP Infrastructure:**
- Created `infrastructure/zap/zap.yaml` (passive scan context config)
- Created `infrastructure/zap/rules.tsv` (10 suppression rules for JSON API false positives: no cookies, no HTML, Helmet handles headers)
- Created `infrastructure/zap/README.md` (scan setup, CI details, severity policy, Mac/Windows `network_mode` workaround)
- Added `zap-scan` service to `docker-compose.test.yml` (profile: zap, network_mode: host)
- Added `security:scan` and `security:scan:ci` npm scripts to `package.json`
- Created `docs/security/.gitkeep`, added ZAP report extensions to `.gitignore`

**Batch 2 — k6 Performance Scripts:**
- `test/performance/qr-verify.k6.js`: 50 VU, 2-min load, **p95 < 200ms** (hard domain SLA for Redis-cached path)
- `test/performance/certification-list.k6.js`: 30 VU, 2-min load, p95 < 500ms
- `test/performance/smoke.k6.js`: 1 VU, 1 iter, no auth — CI-safe liveness + readiness + QR reachability
- `test/performance/README.md`: install instructions, env vars table, threshold rationale
- Added `perf:smoke`, `perf:qr`, `perf:list`, `perf:all` npm scripts

**Batch 3 — CI Workflows + Backlog:**
- `.github/workflows/security.yml`: ZAP passive scan on push:main + weekly Monday 04:00 UTC; fails build on High/Critical alerts via `jq` parse of `zap-report.json`
- `.github/workflows/performance.yml`: smoke job on every push; load job weekly or workflow_dispatch(full); k6 installed via apt
- `PRODUCT-BACKLOG.md`: US-091–103 all marked Done; section 9e added (US-104/105, 5 SP each); Epic 9 total 65 → 75 SP; Phase 2 sprint table fully updated; summary table updated (15 stories, 75 SP, all Done)

## Verification results

All 3 batches passed: lint ✅ (0 errors), typecheck ✅, test:unit ✅ (436/436, 45 suites).

## Key decisions

1. ZAP: passive scan only — `zap-api-scan.py` reads `openapi.json`; no active injection; `network_mode: host` for Linux CI; Mac/Windows workaround documented
2. k6 thresholds: QR verify p95 < 200ms is non-negotiable (domain SLA, Redis-cached); cert list p95 < 500ms is a soft target
3. k6 smoke runs every push; load tests run weekly — avoids CI noise while keeping fast-path coverage
4. curl retry loop (30 × 2s) for API readiness in CI — no `wait-on` npm dependency needed
5. P2-S4 scope corrected in backlog: delivered US-100–103 (23 SP), not just US-100/101 (13 SP) as originally planned

## Blockers

None.

## What's next

**Phase 2 is COMPLETE** — all 5 sprints done, 75 SP, 15 stories.

- Run live validations: `npm run security:scan`, `npm run perf:smoke`, Playwright E2E (require Docker stack)
- Phase 3 planning: microservice extraction, Kubernetes, contract testing with Pact
- Run avro round-trip integration test: `docker compose --profile core up -d && npm run test:integration -- --testPathPattern=avro-roundtrip`
