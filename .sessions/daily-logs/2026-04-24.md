# Daily Log — 2026-04-24

## Session Context

- **Session ID:** session-2026-04-24-FE-S10-complete-v1-done
- **Sprint completed this session:** FE-S10 (15 SP) — **v1 COMPLETE**

---

## What Was Done

### FE-S10 — Customs-Agent Portal + E2E + Docker (15 SP, 19 files, 45 routes)

#### Batch 1 — Backend patch + customs-agent portal core (4 tasks)

- `export-document.controller.ts` — added `customs-agent` to `@Roles` on `GET /export-documents`; 391/391 backend unit tests passing
- `customs-agent/page.tsx` — RSC dashboard: 3 stat cards (total docs, submitted/pending, approved); data from `GET /api/v1/export-documents?page=1&limit=100`
- `export-documents/page.tsx` — RSC paginated list: 9-column table (ID, destinataire, pays, HS code, quantité, réf. ONSSA, statut, date, examiner link), "+ Générer" action button
- `export-documents/[id]/page.tsx` — RSC detail: 2 stat grids + PDF download link + conditional `ValidateForm` (shown only when `status === 'submitted'`)
- `export-documents/[id]/actions.ts` — `validateDocument(id)` Server Action: `POST /api/v1/export-documents/:id/validate` + `revalidatePath`
- `export-documents/[id]/validate-form.tsx` — client component: single "Valider le dédouanement" button with `useTransition` + error state

#### Batch 2 — Generate form + StatusBadge polish (4 tasks)

- `export-documents/new/page.tsx` + `generate-form.tsx` — client form: 6 fields (certificationId, destinationCountry, hsCode, quantityKg, consigneeName, consigneeCountry), success banner with link to created doc
- `apps/portal/src/app/api/generate-export-doc/route.ts` — Next.js API route proxy: POST to backend with server-side `getAccessToken()` Bearer header; avoids CORS and keeps auth logic off the client
- `status-badge.tsx` — extended COLORS map with 5 ExportDocument statuses: `draft`→gray, `submitted`→yellow, `approved`→green, `rejected`→red, `expired`→slate
- 4 unescaped apostrophe lint errors fixed (`d'exportation`, `d'export`, `jusqu'au`, `l'exportation`)
- Build: 45 portal routes + 4 public routes, 0 lint errors, 0 type errors ✅

#### Batch 3 — Playwright E2E (4 tasks)

- `@playwright/test` installed at workspace root (`pnpm add -D -w`); `playwright.config.ts` wired to port 3001, testDir `apps/portal/e2e/`
- `test:e2e` script added to root `package.json`
- `e2e/auth.spec.ts` — 4 tests: customs-agent list redirect, dashboard redirect, login page renders, unauthorized page renders
- `e2e/qr-verify.spec.ts` — 3 tests: unknown UUID doesn't 500, body renders, Arabic locale works
- `e2e/export-documents.spec.ts` — 4 tests: list/new/detail/dashboard all redirect unauthenticated

#### Batch 4 — Docker prod build (3 tasks)

- `apps/portal/Dockerfile` — rewritten: 4-stage (base/deps/builder/runner), repo-root build context, workspace manifests COPY first for layer caching, `pnpm build` from `/repo/apps/portal`
- `apps/public/Dockerfile` — same pattern, port 3002
- `.dockerignore` — excludes `node_modules`, `**/node_modules`, `.next`, `**/.next`
- `docker-compose.prod.yml` — portal:3001 + public:3002, env vars wired (NEXTAUTH_SECRET, KEYCLOAK_*, NEXT_PUBLIC_API_URL)

---

## Ceremonies

- `/retro` — FE-S10 retro written to `.sessions/sprint-logs/FE-S10-retro.md`; velocity tracker updated (FE-S10: 15/15 SP, 100%)
- `/daily-standup` — generated for 2026-04-24
- `/sprint-status` — full dashboard generated (all 10 FE sprints complete, 127 SP, 45 routes, 100% velocity)

---

## Commits (terroir-ma-web)

| Hash | Message |
|------|---------|
| `98f4dfe` | feat(portal): customs-agent export-documents portal (FE-S10) |
| `eb3a48d` | test(e2e): add Playwright smoke tests for auth + QR verify + export docs |
| `bbd8a7f` | chore(docker): fix pnpm monorepo Dockerfiles + add docker-compose.prod.yml |
| `37fed48` | chore(session): save state 2026-04-24 — FE-S10 complete |

## Commits (terroir-ma backend)

| Hash | Message |
|------|---------|
| `eb3a48d` | feat(certification): allow customs-agent to list all export documents |

---

## Bugs Fixed

| Bug | Fix |
|-----|-----|
| `GET /export-documents` blocked customs-agent (super-admin only) | Added `customs-agent` to `@Roles` on controller |
| 4× unescaped apostrophes in JSX (`d'exportation`, `d'export`, `jusqu'au`, `l'exportation`) | Replaced with `&apos;` HTML entity |
| Original Dockerfiles copied `pnpm-lock.yaml` from app dir (doesn't exist in pnpm monorepo) | Rewritten to use repo-root build context |

---

## Decisions Made

1. Backend `GET /export-documents` patched first (Task 1) — no frontend list page is possible without it
2. `/api/generate-export-doc` Next.js route proxy attaches Bearer token server-side — `GenerateExportDocForm` stays a dumb form
3. `ValidateForm` conditionally shown (`status === 'submitted'`) — `approved` shows a banner instead; no dead UI for other statuses
4. Playwright installed at workspace root (`-w`) — single `playwright.config.ts` for all apps
5. Dockerfiles use `FROM node:20-alpine AS base` shared stage — `corepack enable` runs once
6. E2E tests are smoke-only (unauthenticated redirects + public page renders) — authenticated flows deferred to Phase 2

---

## Test Status

- Unit: 391/391 ✅
- Integration: 29/29 ✅
- E2E (Supertest): 35/35 ✅
- Playwright: 11 smoke tests written (run against live apps)

---

## Next Session (original — v1 complete)

**v1 is COMPLETE.** Phase 2 planning options:
1. `/brainstorm` — Prometheus + Grafana monitoring stack
2. `/brainstorm` — Jaeger distributed tracing
3. `/brainstorm` — Kafka Schema Registry + Avro
4. Verify Docker prod build: `docker compose -f docker-compose.prod.yml up --build`
5. Configure Keycloak dev fixture for authenticated Playwright E2E flows

---

# Phase 2 Session — 2026-04-24 (continued same day)

## Session Context (Phase 2 continuation)

- **Session ID:** session-2026-04-24-P2-S1-S2-observability
- **Sprints completed:** P2-S1 (11 SP) + P2-S2 (13 SP) = **24 SP Phase 2**

---

## What Was Done (Phase 2)

### Brainstorm × 4 — Phase 2 Features Designed

1. **Prometheus + Grafana** (Option A) — `docs/plans/2026-04-24-phase2-monitoring/design.md`
2. **Jaeger Distributed Tracing** (Option C) — `docs/plans/2026-04-24-phase2-tracing/design.md`
3. **Kafka Schema Registry + Avro** (Option B) — `docs/plans/2026-04-24-phase2-schema-registry/design.md`
4. **Authenticated Playwright E2E** (Options C+D) — `docs/plans/2026-04-24-phase2-e2e-auth/design.md`

### Backlog Update

- Added Epic 9 (13 stories, US-091–US-103, 65 SP) to `PRODUCT-BACKLOG.md`
- Fixed summary table (7 stale cells — now 88 done / 0 deferred-at-time / 13 Phase 2 todo)
- Added Phase 2 Sprint Plan section (P2-S1 → P2-S5)

### P2-S1 — Prometheus + Grafana (11 SP, 391→397 tests)

**Files created (11):**
- `src/common/metrics/metrics.service.ts` — `prom-client` Histogram + Counter, custom `Registry` per instance
- `src/common/metrics/http-metrics.interceptor.ts` — global interceptor via `tap()`, route normalization via `req.route?.path`
- `src/common/metrics/metrics-ip.guard.ts` — `CanActivate` allowing 127.x / ::1 / 172.x / 10.x
- `src/common/metrics/metrics.controller.ts` — `GET /metrics`, `@UseGuards(MetricsIpGuard)`
- `src/common/metrics/metrics.module.ts` — registers `APP_INTERCEPTOR`, exports `MetricsService`
- `infrastructure/prometheus/prometheus.yml`
- `infrastructure/grafana/provisioning/datasources/prometheus.yml`
- `infrastructure/grafana/provisioning/dashboards/terroir.yml`
- `infrastructure/grafana/dashboards/terroir-api.json` — 6 panels (rate, P95, P99, 5xx, 4xx, slowest routes)
- `test/unit/common/metrics.service.spec.ts` (3 tests)
- `test/unit/common/http-metrics.interceptor.spec.ts` (3 tests)

**Files modified (3):** `src/app.module.ts`, `infrastructure/docker/docker-compose.yml`, `.env.example`

**Key decision:** `new Registry()` per `MetricsService` instance — prevents global prom-client registry conflicts across Jest test modules.

**Bug fixed:** `interface RouteAwareRequest extends Request { route? }` → TypeScript error (Express `Request.route` is required, not optional). Fixed by casting inline: `(req.route as { path?: string } | undefined)?.path`.

### P2-S2 — Jaeger Distributed Tracing (13 SP, 397→400 tests)

**Files created (3):**
- `src/tracing.ts` — `NodeSDK` with `OTLPTraceExporter`, `getNodeAutoInstrumentations()` (fs+dns disabled), SIGTERM shutdown
- `src/common/logger/pino-otel-mixin.ts` — `otelMixin()` returns `{ traceId, spanId }` from active OTel span or `{}`
- `test/unit/common/pino-otel-mixin.spec.ts` (3 tests)

**Files modified (4):** `src/main.ts` (import first line), `src/app.module.ts` (customProps), `infrastructure/docker/docker-compose.yml` (terroir-jaeger), `.env.example` × 2

**Key decisions:**
- `import './tracing'` must be absolute first line of `main.ts` — OTel patches Node.js require hooks
- `OTEL_SDK_DISABLED=true` default in root `.env.example`, `false` in `infrastructure/docker/.env.example`

**Known issue:** Jest worker teardown warning after OTel install — OTel gRPC exporter background timer prevents graceful worker exit; non-blocking (all 400 tests pass).

---

## Ceremonies (Phase 2)

- `/daily-standup` — generated for P2-S1+S2 session
- `/retro` — combined P2-S1+P2-S2 retro written to `.sessions/sprint-logs/P2-S1-S2-retro.md`; velocity tracker updated (P2-S1: 11 SP 100%, P2-S2: 13 SP 100%)
- `/sprint-status` — Phase 2 dashboard generated (37% complete, 24/65 SP done)

---

## Commits (terroir-ma, this Phase 2 session)

| Hash | Message |
|------|---------|
| `4609d82` | chore(session): save state 2026-04-23 — FE-S7 FE-S8 FE-S9 complete |
| `088027a` | chore(session): save state 2026-04-24 — FE-S10 complete v1 DONE |
| _(pending)_ | feat(metrics): Prometheus + Grafana monitoring — P2-S1 |
| _(pending)_ | feat(tracing): Jaeger OTel distributed tracing — P2-S2 |
| _(pending)_ | chore(session): save state 2026-04-24 — P2-S1 P2-S2 complete |

---

## Test Status (end of session)

- Unit: **400/400** ✅ (+9 from P2-S1+S2)
- Integration: 29/29 ✅ (unchanged)
- E2E (Supertest): 35/35 ✅ (unchanged)
- Playwright: 11 smoke tests ✅ (unchanged)

---

## Next Session

**Phase 2 — P2-S3: Kafka Schema Registry + Avro (18 SP)**
- Stories: US-097, US-098, US-099
- Goal: 27 Kafka events migrated to Avro, Schema Registry enforces BACKWARD compat, integration test proves round-trip encode/decode
- Action items before starting: run `docker compose --profile monitoring up -d` and verify Prometheus scrape + Grafana panels + Jaeger traceId in logs
