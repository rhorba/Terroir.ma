# Daily Log — 2026-04-09

## Sprint 1 — Close-out session

### What was accomplished

**Full test suite green: 85 tests passing, 0 TypeScript errors**

- 11/11 unit test suites (58 tests)
- 5/5 integration test suites (11 tests)
- 4/4 e2e test suites (16 tests)

### Key fixes this session

#### TypeScript / entity fixes
- Added `type: 'varchar'` to 16 nullable string columns across 9 entity files (reflect-metadata emits `Object` for `string | null` union types without explicit type)
- Fixed `faker.internet.username()` → `faker.internet.userName()`
- Fixed `noUncheckedIndexedAccess` violations in database helper

#### Infrastructure
- Created `KafkaClientModule` (`@Global()`) to provide `KAFKA_CLIENT` token to all domain modules
- Installed `pino-pretty` as dev dependency (NestJS logger requires it in non-production mode)
- Fixed `testTimeout` — must be at root config level in Jest, not inside `projects[]`

#### Test factories / fixtures
- Rewrote `cooperative.factory.ts`, `harvest.factory.ts`, `certification.factory.ts`, `product.factory.ts` to match actual entity fields
- Rewrote `cooperative.fixture.ts` with correct snake_case DB column names
- Rewrote `notification-templates.fixture.ts` with snake_case keys

#### Integration tests
- Fixed Testcontainers setup to create schemas from entity metadata before `synchronize()`
- Fixed notification integration `afterEach` to only truncate `notification.notification` (not wipe templates)

#### E2E tests
- Created `TestJwtStrategy` (passport-custom) registered as `'jwt'` to bypass Keycloak in e2e
- Added mock Kafka client to `createTestApp()` via `.overrideProvider('KAFKA_CLIENT')`
- Updated `createTestApp()` to create schemas + call `dataSource.synchronize()` after `app.init()`
- Fixed QR verify controller: `sig` query param → 403 on mismatch; no sig + not found → 404

### Decisions
- E2E tests now self-initialize the DB (schema creation + synchronize) on each `createTestApp()` call
- QR verification endpoint design: path param = HMAC (direct) OR UUID (with sig query param for explicit verification)
